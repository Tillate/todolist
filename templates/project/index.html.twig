{% extends "layouts/base.html.twig" %}

{% block title %}Liste des projets{% endblock %}

{% block body %}
  <!-- Primary Page Layout
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
<div class="container-tab">
    <div class="row">
        <div class="one-half column" style="margin-top: 2%">
            <h4>Liste des projets</h4>
        </div>
    </div>
    <a class="button button-primary" href="/project/add">Ajouter un projet</a>
        <div class="row">
          <div class="column">
          </div>
        </div>
        <div class="row">
            <div class="column">
                <table class="u-full-width">
                    <thead>
                        <tr>
                            <th>Nom</th>
                            <th>Description</th>
                            <th>Début</th>
                            <th>Fin</th>
                            <th>Ajouter un utilisateur</th>
                            <th>Modifier un projet</th>
                            <th>Ajouter une tâches</th>
                        </tr>
                    </thead>
                    <tbody>
{% for project in projects %}
                        <tr>
                            <td>{{ project.getName }}</td>
                            <td>{{ project.getDescription }}</td>
                            <td>{{ project.getStartDate|date("d M Y") }}</td>
                            <td>{{ project.getEndDate|date("d M Y") }}</td>
                            <td><a href="/project/{{ project.getId }}/user" class="button button-primary">Ajouter</a></td>
                            <td><a href="/project/edit/{{ project.getId }}" class="button button-primary">Modifier</a></td>
                            <td><a href="/project/{{ project.getId }}/task" class="button button-primary">Ajouter</a></td>
                        </tr>
{% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
</div>
<!-- End Document
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
{% endblock %}

